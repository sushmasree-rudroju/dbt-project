version: 2

sources:
  - name: stripe
    description: This a replica of the Postgres database used by our app
    database: raw
    schema: stripe
    tables:
      - name: payment
        description: One record per order. Includes payment details
        config:
          freshness:
            warn_after:
              count: 48
              period: hour
            error_after:
              count: 12
              period: day
          loaded_at_field: _batched_at